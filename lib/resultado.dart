import 'package:flutter/material.dart';

class MyWidget extends StatelessWidget {
  String texto;

  MyWidget(this.texto, {super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}